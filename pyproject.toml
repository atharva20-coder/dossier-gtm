[project]
name = "dossier"
version = "0.1.0"
requires-python = ">=3.12"

[tool.vercel]
# The FastAPI instance lives in backend/main.py; Vercel loads it directly, so
# there is no adapter shim and the app deploys exactly as it runs locally.
entrypoint = "backend.main:app"
