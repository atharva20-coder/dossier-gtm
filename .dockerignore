# .env holds the Gmail app password, the database URL and every provider key.
# It must never enter the image — and if it did, load_dotenv would override the
# variables Railway injects.
.env
.env.local
*.env.local

.git
node_modules
ui/node_modules
frontend
*.db
*.sqlite3
*.tsbuildinfo
__pycache__
*.pyc
.pytest_cache
.venv
venv
docs
# …except the landing page, which the app serves at /. Nothing else
# in docs/ ships: it is interview prep, not public material.
!docs/pitch
!docs/pitch/index.html
!docs/pitch/limitations.html
!docs/pitch/future-scope.html
fixtures
supabase
scripts
tools
test_*.py
verify.py
discover_supercarl.py
.DS_Store
